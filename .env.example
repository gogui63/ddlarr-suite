# Server configuration
PORT=9117
HOST=0.0.0.0

# Site URLs (required - at least one)
WAWACITY_URL=
ZONETELECHARGER_URL=
DARKIWORLD_URL=

# DarkiWorld API key (optional - required if using darkiworld)
DARKIWORLD_API_KEY=

# AllDebrid API key (optional - if not set, uses Botasaurus for dl-protect resolution)
ALLDEBRID_API_KEY=

# DL-Protect resolver service URL (optional - defaults to http://localhost:5000)
# In docker-compose, this is set to http://dlprotect-resolver:5000
DLPROTECT_SERVICE_URL=http://localhost:5000
